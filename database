<?php
session_start();
$db_name="login";
$host="localhost";
$username="root";
$password="";

$con=mysqli_connect($host,$username,$password,$db_name);
if(!$con)
{
    echo "Not Connected";
   }
   else
{
    //echo "Connected";
}
?>
